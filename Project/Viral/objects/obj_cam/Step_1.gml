
x = obj_char.x;
y = obj_char.y;


/*if (instance_exists(obj_char))
{
    var dist = point_distance(obj_char.x, obj_char.y, mouse_x, mouse_y);
    
    if (dist < dist_max)
    {
        targ_x = obj_char.x;
        targ_y = obj_char.y;
    }
    else
    {
        var ang = point_direction(obj_char.x, obj_char.y, mouse_x, mouse_y);
        
        targ_x = obj_char.x + lengthdir_x(dist_max, ang);
        targ_y = obj_char.y + lengthdir_y(dist_max, ang);
    }
}

x = targ_x;
y = targ_y;*/


/*var dist = point_distance(x, y, targ_x, targ_y);

if (dist > 4)
{
    var ang = point_direction(obj_char.x, obj_char.y, mouse_x, mouse_y);
    
    x -= lengthdir_x(dist/2, ang);
    y -= lengthdir_y(dist/2, ang);
}*/
